@model string[]

@{
    ViewData["Title"] = "Log";
}


<div class="wrapper wrapper-content animated fadeIn" style="animation-delay: 0.4s;">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Log </h5>
                </div>
                <div class="ibox-content">
                    @foreach (string l in Model)
                    {
                        @Html.Raw(l)
                        <br />
                        <br />
                    }

                </div>
            </div>
        </div>
    </div>
</div>
